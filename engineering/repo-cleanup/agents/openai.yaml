interface:
  display_name: "Repository Cleanup"
  short_description: "Audit and safely clean repository artifacts"
  default_prompt: "Use $repo-cleanup to audit this repository and prepare a safe cleanup plan."
policy:
  allow_implicit_invocation: false
